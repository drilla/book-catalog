{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_group_new'), 'attr': { 'class': 'fos_user_group_new' } }) }}
    {{ form_widget(form) }}
    <div class="form-group">
        <input class="btn btn-dark" type="submit" value="{{ 'group.new.submit'|trans }}" />
    </div>
{{ form_end(form) }}
