<header>
    <div class="container">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li class="nav-item active">
                            <a class="nav-link" href="{{ url('book_index') }}">Книги <span class="sr-only">(current)</span></a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ url('author_index') }}">Авторы</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ url('genre_index') }}">Жанры</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ url('fos_user_profile_show') }}">Пользователь</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link" href="{{ url('fos_user_security_logout') }}">Выйти</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ url('fos_user_security_login') }}">Войти</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ url('fos_user_registration_register') }}">Регистрация</a>
                        </li>
                    {% endif %}

                </ul>
            </div>
        </nav>
    </div>
</header>