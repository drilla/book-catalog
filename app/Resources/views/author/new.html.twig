{% extends 'layout/base.html.twig' %}

{% block content %}
    <h1>Author creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <a class="btn btn-dark" href="{{ path('author_index') }}">Back to the list</a>

{% endblock %}
